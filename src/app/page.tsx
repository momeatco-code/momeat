export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-5xl flex-col justify-center px-6 py-16 sm:px-10">
      <p className="text-sm font-medium tracking-[0.18em] text-amber-700 uppercase">
        MomEat
      </p>
      <h1 className="mt-4 max-w-2xl text-4xl font-semibold tracking-tight text-stone-950 sm:text-6xl">
        Foundation ready for the first product flow.
      </h1>
      <p className="mt-6 max-w-xl text-lg leading-8 text-stone-600">
        The technical base is modular, secure by default, and intentionally
        excludes payments and irreversible financial operations.
      </p>
    </main>
  );
}
